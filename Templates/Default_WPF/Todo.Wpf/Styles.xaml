<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="clr-namespace:<#cs Write(Project.Namespace)#>.Wpf">

	<SolidColorBrush x:Key="BackgroundColor" Color="<#cs Write(Project.GetSetting("PrimaryColor"))#>" />
	<SolidColorBrush x:Key="ForegroundColor" Color="<#cs Write(Project.GetSetting("ForegroundColor"))#>" />
	<SolidColorBrush x:Key="ValidationErrorColor" Color="#B42318" />

	<ControlTemplate x:Key="ValidationErrorTemplate">
		<DockPanel LastChildFill="True">
			<TextBlock DockPanel.Dock="Bottom"
					   Margin="0,0,0,5"
					   Foreground="{StaticResource ValidationErrorColor}"
					   Text="{Binding [0].ErrorContent}"
					   TextWrapping="Wrap" />
			<AdornedElementPlaceholder />
		</DockPanel>
	</ControlTemplate>

	<Style x:Key="ValidatedInputStyle" TargetType="{x:Type Control}">
		<Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationErrorTemplate}" />
		<Style.Triggers>
			<Trigger Property="Validation.HasError" Value="True">
				<Setter Property="BorderBrush" Value="{StaticResource ValidationErrorColor}" />
			</Trigger>
		</Style.Triggers>
	</Style>

	<Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource ValidatedInputStyle}" />
	<Style TargetType="{x:Type PasswordBox}" BasedOn="{StaticResource ValidatedInputStyle}" />
	<Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource ValidatedInputStyle}" />
	<Style TargetType="{x:Type DatePicker}" BasedOn="{StaticResource ValidatedInputStyle}" />

	<Style TargetType="{x:Type Button}">
		<Setter Property="Foreground" Value="{StaticResource ForegroundColor}" />
		<Setter Property="Background" Value="{StaticResource BackgroundColor}" />
		<Setter Property="BorderThickness" Value="0" />

		<Setter Property="Padding" Value="10,5" />

		<Style.Triggers>
			<Trigger Property="IsMouseOver" Value="True">
				<Setter Property="Foreground" Value="{StaticResource BackgroundColor}" />
			</Trigger>
		</Style.Triggers>
	</Style>

	<Style TargetType="{x:Type DataGridColumnHeader}">
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="Background" Value="{StaticResource BackgroundColor}" />
		<Setter Property="Padding" Value="5,2" />
		<Setter Property="Foreground" Value="{StaticResource ForegroundColor}" />
		<Setter Property="BorderBrush" Value="{StaticResource ForegroundColor}" />
		<Setter Property="BorderThickness" Value="0,0,1,0" />
	</Style>

    <Style TargetType="{x:Type DataGridCell}">
        <Setter Property="Foreground" Value="{StaticResource BackgroundColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="False" />
    </Style>
</ResourceDictionary><#cs SetOutputPathAndSkipOtherDefinitions()#>