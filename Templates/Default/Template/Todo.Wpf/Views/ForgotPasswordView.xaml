<UserControl x:Class="<#cs Write(Project.Namespace)#>.Wpf.Views.ForgotPasswordView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:loc="clr-namespace:<#cs Write(Project.Namespace)#>.Client.Localisation;assembly=<#cs Write(Project.Namespace)#>.Client.Localisation"
			 mc:Ignorable="d"
			 d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Border MaxWidth="400"
				Padding="32"
				HorizontalAlignment="Center"
				VerticalAlignment="Center">
			<StackPanel>
				<TextBlock Margin="0,0,0,16"
						   HorizontalAlignment="Center"
						   FontSize="24"
						   FontWeight="Bold"
						   Foreground="{StaticResource BackgroundColor}"
						   Text="{x:Static loc:UserLoc.ForgotPassword}" />

				<StackPanel Margin="0,0,0,16">
					<TextBlock Margin="0,0,0,4" Text="{x:Static loc:UserLoc.EmailAddress}" />
					<TextBox Text="{Binding EMail, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />
				</StackPanel>

				<Button Margin="0,8,0,0"
						HorizontalAlignment="Stretch"
						HorizontalContentAlignment="Center"
						Command="{Binding RequestResetCodeCommand}"
						Content="{x:Static loc:UserLoc.SendResetCode}" />

				<Button Margin="0,8,0,0" Command="{Binding NavigateToLoginCommand}">
					<TextBlock Text="{x:Static loc:UserLoc.Login}" />
				</Button>
			</StackPanel>
		</Border>
	</Grid>
</UserControl><#cs SetOutputPathAndSkipOtherDefinitions()#>