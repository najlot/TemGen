<UserControl x:Class="<#cs Write(Project.Namespace)#>.Wpf.Views.TrashView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:loc="clr-namespace:<#cs Write(Project.Namespace)#>.Client.Localisation;assembly=<#cs Write(Project.Namespace)#>.Client.Localisation"
			 xmlns:local="clr-namespace:<#cs Write(Project.Namespace)#>.Wpf.Views"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:converter="clr-namespace:<#cs Write(Project.Namespace)#>.Wpf.Converters"
			 xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
			 d:DesignHeight="450"
			 d:DesignWidth="800"
			 mc:Ignorable="d">
	<Grid>
		<DockPanel>
			<DockPanel DockPanel.Dock="Top">
				<Button Margin="5" DockPanel.Dock="Left" Command="{Binding NavigateBackCommand}">
					<Button.Content>
						<materialIcons:MaterialIcon Kind="ArrowLeft" Width="18" Height="18" />
					</Button.Content>
				</Button>

				<Button HorizontalAlignment="Right"
						DockPanel.Dock="Right"
						Margin="5"
						Command="{Binding DeleteAllTrashCommand}"
						ToolTip="{x:Static loc:TrashLoc.DeleteAll}">
						<Button.Content>
							<materialIcons:MaterialIcon Kind="DeleteSweep" Width="18" Height="18" />
						</Button.Content>
				</Button>

				<DockPanel DockPanel.Dock="Left">
					<Label VerticalAlignment="Center" Content="{x:Static loc:CommonLoc.Filter}" />
					<TextBox Margin="5"
							 VerticalAlignment="Center"
							 Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}" />
				</DockPanel>
			</DockPanel>

			<ScrollViewer>
				<ItemsControl ItemsSource="{Binding TrashItemsView}" HorizontalAlignment="Stretch">
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<Border Margin="6" Padding="10" CornerRadius="8" Background="#FFF6F6F6" BorderBrush="#FFE0E0E0" BorderThickness="1" HorizontalAlignment="Stretch">
								<StackPanel>
									<DockPanel>
										<TextBlock DockPanel.Dock="Right" Foreground="#FF666666" Text="{Binding DeletedAtDisplay}" />
										<TextBlock FontWeight="SemiBold" Text="{Binding Title}" />
									</DockPanel>
									<TextBlock Margin="0,4,0,0" Foreground="#FF444444" Text="{Binding Content}" TextWrapping="Wrap" />
									<TextBlock Margin="0,6,0,0" Foreground="#FF666666" Text="{Binding TypeDisplay}" />
									<WrapPanel Margin="0,8,0,0">
										<Button Margin="0,0,8,0"
												Command="{Binding DataContext.OpenTrashItemCommand, RelativeSource={RelativeSource AncestorType=local:TrashView}}"
												CommandParameter="{Binding .}"
												Content="{x:Static loc:CommonLoc.Edit}" />
										<Button Margin="0,0,8,0"
												Command="{Binding DataContext.RestoreTrashItemCommand, RelativeSource={RelativeSource AncestorType=local:TrashView}}"
												CommandParameter="{Binding .}"
												Content="{x:Static loc:TrashLoc.Restore}" />
										<Button Command="{Binding DataContext.DeleteTrashItemCommand, RelativeSource={RelativeSource AncestorType=local:TrashView}}"
												CommandParameter="{Binding .}"
												Content="{x:Static loc:CommonLoc.Delete}" />
									</WrapPanel>
								</StackPanel>
							</Border>
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>
			</ScrollViewer>
		</DockPanel>

		<local:DeleteConfirmationDialogView Visibility="{Binding IsVisible, Converter={x:Static converter:BooleanToVisibilityConverter.Instance}}"
								 DataContext="{Binding DeleteDialogViewModel}" />
	</Grid>
</UserControl>
<#cs SetOutputPathAndSkipOtherDefinitions()#>