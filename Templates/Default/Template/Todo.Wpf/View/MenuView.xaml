<UserControl x:Class="<#cs Write(Project.Namespace)#>.Wpf.View.MenuView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:local="clr-namespace:<#cs Write(Project.Namespace)#>.Wpf.View"
			 xmlns:loc="clr-namespace:<#cs Write(Project.Namespace)#>.Client.Localisation;assembly=<#cs Write(Project.Namespace)#>.Client.Localisation"
			 mc:Ignorable="d"
			 d:DesignHeight="450" d:DesignWidth="800">
	<DockPanel HorizontalAlignment="Center" VerticalAlignment="Center">
		<StackPanel>
<#cs
foreach(var definition in Definitions.Where(d => !d.IsOwnedType
&& !d.IsArray
&& !d.IsEnumeration
&& !d.Name.Equals("user", StringComparison.OrdinalIgnoreCase)))
{
	WriteLine($"			<Button Content=\"{definition.Name}s\" Margin=\"5\" Padding=\"10,5\" Command=\"{{Binding NavigateTo{definition.Name}s}}\" />");
}

Result = Result.TrimEnd();
#>
		</StackPanel>
	</DockPanel>
</UserControl>
<#cs SetOutputPathAndSkipOtherDefinitions()#>