<UserControl x:Class="<#cs Write(Project.Namespace)#>.Wpf.View.RmqProfileView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:local="clr-namespace:<#cs Write(Project.Namespace)#>.Wpf.View"
			 xmlns:loc="clr-namespace:<#cs Write(Project.Namespace)#>.Client.Localisation;assembly=<#cs Write(Project.Namespace)#>.Client.Localisation"
			 mc:Ignorable="d"
			 d:DesignHeight="450" d:DesignWidth="800">
	<StackPanel>
		<Label Content="{x:Static loc:ProfileLoc.RmqHost}" />
		<TextBox Text="{Binding RabbitMqHost}" />

		<Label Content="{x:Static loc:ProfileLoc.RmqVirtualHost}" />
		<TextBox Text="{Binding RabbitMqVirtualHost}" />

		<Label Content="{x:Static loc:ProfileLoc.RmqUser}" />
		<TextBox Text="{Binding RabbitMqUser}" />

		<Label Content="{x:Static loc:ProfileLoc.RmqPassword}" />
		<TextBox Text="{Binding RabbitMqPassword}" />

		<Label Content="{x:Static loc:ProfileLoc.User}" />
		<TextBox Text="{Binding ServerUser}" />

		<Label Content="{x:Static loc:ProfileLoc.Password}" />
		<TextBox Text="{Binding ServerPassword}" />
	</StackPanel>
</UserControl><#cs SetOutputPathAndSkipOtherDefinitions()#>