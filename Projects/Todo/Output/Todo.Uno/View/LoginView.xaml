<UserControl x:Class="Todo.Uno.View.LoginView"
			 x:Name="This"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:loc="using:Todo.Client.Localisation"
			 mc:Ignorable="d"
			 d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Border MaxWidth="400"
				Padding="32"
				HorizontalAlignment="Center"
				VerticalAlignment="Center"
				BorderBrush="{StaticResource AppBackgroundColor}"
				BorderThickness="1">
			<StackPanel>
				<TextBlock Margin="0,0,0,16"
						   HorizontalAlignment="Center"
						   FontSize="24"
						   FontWeight="Bold"
						   Foreground="{StaticResource AppBackgroundColor}"
						   Text="{x:Static loc:CommonLoc.Login}" />

				<!--  Username  -->
				<StackPanel Margin="0,0,0,16">
					<TextBlock Margin="0,0,0,4" Text="{x:Static loc:CommonLoc.Username}" />
					<TextBox x:Name="UsernameTextBox" Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
				</StackPanel>

				<!--  Password  -->
				<StackPanel Margin="0,0,0,16">
					<TextBlock Margin="0,0,0,4" Text="{x:Static loc:CommonLoc.Password}" />
					<PasswordBox x:Name="PasswordBox"
								 PasswordChanged="PasswordBox_PasswordChanged" />
				</StackPanel>

				<!--  Login Button  -->
				<Button Margin="0,8,0,0"
						HorizontalAlignment="Stretch"
						HorizontalContentAlignment="Center"
						Command="{Binding LoginCommand}"
						Content="{x:Static loc:CommonLoc.Login}" />

				<!--  Navigate to Register Link  -->
				<Button Margin="0,8,0,0" Command="{Binding NavigateToRegisterCommand}">
					<TextBlock Text="{x:Static loc:CommonLoc.Register}" />
				</Button>
			</StackPanel>
		</Border>
	</Grid>
</UserControl>

