<UserControl x:Class="Todo.Wpf.Views.RegisterView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:loc="clr-namespace:Todo.Client.Localisation;assembly=Todo.Client.Localisation"
			 xmlns:controls="clr-namespace:Todo.Wpf.Controls"
			 mc:Ignorable="d"
			 d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Border MaxWidth="400"
				Padding="32"
				HorizontalAlignment="Center"
				VerticalAlignment="Center">
			<StackPanel>
				<TextBlock Margin="0,0,0,16"
						   HorizontalAlignment="Center"
						   FontSize="24"
						   FontWeight="Bold"
						   Foreground="{StaticResource BackgroundColor}"
						   Text="{x:Static loc:UserLoc.Register}" />

				<!--  Username  -->
				<StackPanel Margin="0,0,0,16">
					<TextBlock Margin="0,0,0,4" Text="{x:Static loc:UserLoc.Username}" />
					<TextBox x:Name="UsernameTextBox" Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />
				</StackPanel>

				<!--  Email  -->
				<StackPanel Margin="0,0,0,16">
					<TextBlock Margin="0,0,0,4" Text="{x:Static loc:UserLoc.EmailAddress}" />
					<TextBox Text="{Binding EMail, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" />
				</StackPanel>

				<!--  Password  -->
				<StackPanel Margin="0,0,0,16">
					<TextBlock Margin="0,0,0,4" Text="{x:Static loc:UserLoc.Password}" />
					<PasswordBox controls:PasswordBoxHelper.BindPassword="True"
								 controls:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, ValidatesOnNotifyDataErrors=True}" />
				</StackPanel>

				<!--  Register Button  -->
				<Button Margin="0,8,0,0"
						HorizontalAlignment="Stretch"
						HorizontalContentAlignment="Center"
						Command="{Binding RegisterCommand}"
						Content="{x:Static loc:UserLoc.Register}" />

				<!--  Navigate to Login Link  -->
				<Button Margin="0,8,0,0" Command="{Binding NavigateToLoginCommand}">
					<TextBlock Text="{x:Static loc:UserLoc.Login}" />
				</Button>
			</StackPanel>
		</Border>
	</Grid>
</UserControl>
