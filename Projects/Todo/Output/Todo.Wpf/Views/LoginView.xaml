<UserControl x:Class="Todo.Wpf.Views.LoginView"
			 x:Name="This"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:local="clr-namespace:Todo.Wpf.Views"
			 xmlns:loc="clr-namespace:Todo.Client.Localisation;assembly=Todo.Client.Localisation"
			 xmlns:controls="clr-namespace:Todo.Wpf.Controls"
			 mc:Ignorable="d"
			 d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.InputBindings>
			<KeyBinding Key="Enter" Command="{Binding LoginCommand}" />
		</Grid.InputBindings>
		<Border MaxWidth="400"
				Padding="32"
				HorizontalAlignment="Center"
				VerticalAlignment="Center">
			<StackPanel>
				<TextBlock Margin="0,0,0,16"
						   HorizontalAlignment="Center"
						   FontSize="24"
						   FontWeight="Bold"
						   Foreground="{StaticResource BackgroundColor}"
						   Text="{x:Static loc:UserLoc.Login}" />

				<!--  Username  -->
				<StackPanel Margin="0,0,0,16">
					<TextBlock Margin="0,0,0,4" Text="{x:Static loc:UserLoc.Username}" />
					<TextBox x:Name="UsernameTextBox" Text="{Binding Username, ValidatesOnNotifyDataErrors=True}" />
				</StackPanel>

				<!--  Password  -->
				<StackPanel Margin="0,0,0,16">
					<TextBlock Margin="0,0,0,4" Text="{x:Static loc:UserLoc.Password}" />
					<PasswordBox controls:PasswordBoxHelper.BindPassword="True"
								 controls:PasswordBoxHelper.BoundPassword="{Binding Password, ValidatesOnNotifyDataErrors=True}" />
				</StackPanel>

				<!--  Login Button  -->
				<Button Margin="0,8,0,0"
						HorizontalAlignment="Stretch"
						HorizontalContentAlignment="Center"
						Command="{Binding LoginCommand}"
						Content="{x:Static loc:UserLoc.Login}" />

				<Button Margin="0,8,0,0" Command="{Binding NavigateToForgotPasswordCommand}">
					<TextBlock Text="{x:Static loc:UserLoc.ForgotPassword}" />
				</Button>

				<!--  Navigate to Register Link  -->
				<Button Margin="0,8,0,0" Command="{Binding NavigateToRegisterCommand}">
					<TextBlock Text="{x:Static loc:UserLoc.Register}" />
				</Button>
			</StackPanel>
		</Border>
	</Grid>
</UserControl>
